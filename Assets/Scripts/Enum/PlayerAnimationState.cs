namespace Enum
{
    public enum PlayerAnimationState
    {
        PlayerIdle,
        PlayerRun,
        PlayerWatering,
        PlayerWateringReverse,
        PlayerWeeding,
        PlayerMows
    }
}